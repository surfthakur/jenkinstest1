pipeline {
     agent any
     stages {
          stage("Compile") {
               steps {
                   echo "step compile"
               }
          }
          stage("Unit test") {
               steps {
                    echo "Unit test"
               }
          }
     }
}